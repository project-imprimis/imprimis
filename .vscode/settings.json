{
  // CCLS path to headers. The "include" commands should match the Makefile.
  "ccls.clang.extraArgs": [
    "-Igame",
    "-Ienet/include",
    "-I/usr/X11R6/include",
    "`sdl2-config --cflags`"
  ]
}
